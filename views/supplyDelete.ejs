<html lang="en">
    <%- include('./header.ejs') %>
    <body>
        <%- include('./navBar.ejs') %>
        <div>
            <form action="/supplies/<%= locals.theId %>?_method=DELETE" method="POST">
                <h2>Are you sure you want to delete this item?</h2>
                <label>Confirm order number
                    <input type="text" id="<%= locals.theId %>" name="order" value="<%= locals.itemID %>">
                </label><br>
                <button>Delete</button>
            </form>
        </div>
    </body>
</html>